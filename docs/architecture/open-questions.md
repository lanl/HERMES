# Open Questions

- What are the two acquisition modalities that must be supported first?
- What are the two analysis modalities that must be supported first?
- Should the Rust unpacker be invoked only as a CLI at first, or should a Python
  extension be planned later with PyO3 or maturin?
- What directory structure should be used for persisted run data outside version
  control?
- Which metadata are required for EMPIR analysis provenance?
