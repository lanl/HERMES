# Open Questions
- Should the Rust unpacker be invoked only as a CLI at first, or should a Python extension be planned later with PyO3 or maturin?
- What would a pymepix acquisition environment look like in the state model, and how would it differ from the SERVAL environment?
- Which metadata are required for EMPIR analysis provenance?
- How do I make sure that analysis is not dependent on the presence of acquisition models? For example, data taken without HERMES should still be analyzable by HERMES. 
