#include "clusterer.h"

namespace hermes_photon_clusterer {

// The clustering, filtering, correction, and output logic is added in the
// following Stage 4 steps. This translation unit currently anchors the static
// library so the CMake project configures, builds, and tests.

}  // namespace hermes_photon_clusterer
